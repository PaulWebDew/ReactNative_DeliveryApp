import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({

    categoryCard:{
        width:100,
        height: 100,
        backgroundColor: '#fff',
        marginEnd: 10,
        elevation: 2,
        borderRadius: 4,
    },
    categoryText:{
        padding: 5,
        fontSize: 12,
        fontWeight: 'bold',
    }
})

export default styles;